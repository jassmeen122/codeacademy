
export interface ProjectSpecs {
  projectName: string;
  description: string;
  primaryColor: string;
  secondaryColor: string;
  sections: string[];
}

export type PreviewMode = 'form' | 'preview';
