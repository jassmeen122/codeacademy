
import React from 'react';
import { CodeEditorWrapper } from './CodeEditorWrapper';

// This is a simple pass-through component to enable lazy loading
const LazyCodeEditorWrapper = () => {
  return <CodeEditorWrapper />;
};

export default LazyCodeEditorWrapper;
